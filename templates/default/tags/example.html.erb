<div class="examples">
  <% if docstring.tags(:example).size == 1 %>
    <h3>Example: <small><%= docstring.tag(:example).name %></small></h3>
    <div class="docstring">
      <pre class="code"><%= html_syntax_highlight docstring.tag(:example).text %></pre>
    </div>
  <% else %>
    <h3>Examples</h3>
    <% docstring.tags(:example).each do |tag| %>
    <dl>
      <dt><%= tag.name %></dt>
      <dd class="docstring">
        <pre class="code"><%= html_syntax_highlight tag.text %></pre>
      </dd>
    </dl>
    <% end %>
  <% end %>
</div>