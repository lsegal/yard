<% if object.has_tag?(:see) %>
  <h3>See Also:</h3>
  <ul class="see">
    <% for tag in object.tags(:see) %>
      <li><%= linkify(tag.name, tag.text || tag.name) %></li>
    <% end %>
  </ul>
<% end %>