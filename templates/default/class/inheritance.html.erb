<div class="section inheritance">
  <ul>
  <%= 
    object.inheritance_tree.reverse.map {|o| 
      "<li>" + (o == object ? h(o.path) : linkify(o)) + "</li>" }.join("<ul>") 
  %>
  <%= "</ul>" * object.inheritance_tree.length %>
</div>