<% if method_listing.size > 0 %>
  <h2>Method Summary</h2>
  <ul class="summary">
    <% method_listing.each do |meth| %>
      <li class="<%= meth.visibility %> <%= meth.has_tag?(:deprecated) ? 'deprecated' : '' %>">
        <%= signature(meth) %>
        <% if meth.aliases.size > 0 %>
        (also: <%= meth.aliases.map {|o| h(o.name(true)) }.join(", ") %>)
        <% end %>
        
        <% if meth.has_tag?(:deprecated) %>
          <span class="summary_desc"><strong>Deprecated.</strong> <%= meth.tag(:deprecated).text %></span>
        <% elsif !meth.docstring.empty? %>
          <span class="summary_desc"><%= htmlify_line meth.docstring.summary %></span>
        <% end %>
      </li>
    <% end %>
  </ul>
<% end %>
