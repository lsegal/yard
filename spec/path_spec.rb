require File.join(File.dirname(__FILE__), 'spec_helper')

describe YARD::Path do
  it "should only create one instance of any given path" 
  
  it "should resolve to an object if it exists in the registry"
  
  it "should return a proxy object if it does not exist in the registry"
  
end